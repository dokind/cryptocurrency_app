import 'package:flutter/material.dart';

class CustomPadding {
  // const CustomPadding();
  static var symetric84 =
      const EdgeInsets.symmetric(horizontal: 8.0, vertical: 4.0);
  static var symetric168 =
      const EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0);
  static var all4 = const EdgeInsets.all(4.0);
  static var all8 = const EdgeInsets.all(8.0);
  static var all16 = const EdgeInsets.all(16.0);
}

class CustomColors {
  static Color grey = Color(0xFF757575);
}
